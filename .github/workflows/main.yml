name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  # checkout:
  #   name: Checkout
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@master
      
  earthly:
    runs-on: ubuntu-latest
    steps:
      - uses: earthly/actions-setup@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          version: "latest" # or pin to an specific version, e.g. "0.8.1"
      - uses: actions/checkout@v2
      - run:  echo "boot=$(earthly bootstrap 2>&1 )\n"
        id: bootstrap
      - uses: actions/checkout@v3
      - name: Check for Earthfile file
        run: (ls Earthfile && echo Found Earthfile) || (echo No Earthfile)
        id: check

      - name: Run Earthly and capture output
        id: earthly
        run: |
          echo "output=$(earthly --ci --push -P +ci 2>&1 )" 
          
      - run: ls -la
      # - name: Code Coverage Summary
      #   uses: irongut/CodeCoverageSummary@v2
      #   with:
      #     filename: earthly_output.log
      #     badge-title: Coverage
      #     coverage-regex: '\(statements\)(?:\s+)?(\d+(?:\.\d+)?%)'
      #     output: both
      # - name: Save Coverage Summary to Output
      #   run: |
      #     # 将 coverage_summary.log 的内容写入到 $GITHUB_OUTPUT
      #     echo "coverage_summary=$(cat coverage_summary.log)" >> $GITHUB_OUTPUT
      #   id: save_coverage_summary
      # - uses: actions-ecosystem/action-regex-match@v2
      #   id: regex-match
      #   with:
      #     text: ${{ steps.earthly.outputs.earthly_output }}
      #     regex: '\(statements\)(?:\s+)?(\d+(?:\.\d+)?%)'
      - run: echo ${{ steps.earthly.outputs.earthly_output }}
      - name: Coverage badge
        uses: RubbaBoy/BYOB@v1.3.0
        with:
          NAME: coverage
          ICON: 'https://badgen.net/codecov/c/gitlab/gitlab-org/gitaly'
          STATUS: ${{ steps.earthly.outputs.earthly_output }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          

      # - name: check test coverage
      #   uses: vladopajic/go-test-coverage@v2
      #   with:
    
          
      #     # Configure action by specifying input parameters individually (option 2).
      #     # If you are using config file (option 1) you shouldn't use these parameters, however
      #     # specifing these action parameters will override appropriate config values.
      #     profile: coverage_result.html
      #     local-prefix: github.com/LittleRed945/cli-test-case
      #     threshold-file: 80
      #     threshold-package: 80
      #     threshold-total: 95